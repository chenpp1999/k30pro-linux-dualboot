# build outputs and local artifacts
out/
tools/m0/out/
*.img
*.cpio.gz
*.tar.gz
*.apk
kernel/
d80/
__pycache__/
*.pyc
# IME data downloads (re-fetch with tools/m1/ime/fetch-data.sh)
tools/m1/ime/data/

# local, author-specific forbidden-token list (never commit real identifiers)
tools/ci/forbidden-local.txt

# M5 installer backups: partition dumps (recovery/boot/misc/super) of a real
# device -- device-specific, never commit.
lmi-install-backup/
*.ramboot-ok
*.buildinfo
